import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.akfly.cardcollectorsimulator',
  appName: 'Card Collector Simulator',
  webDir: 'www'
};

export default config;
